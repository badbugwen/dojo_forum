<div class="container">
<%# Category link list  %>
  <nav class="nav">
    <%= link_to 'All', root_path, class:"nav-link active" %>
    <% Category.all.each do |category| %>
      <%= link_to category.name, category_path(category), class:"nav-link" %>
    <% end %>
  </nav>
<%# post table  %>
  <table class="table table-hover">
  <thead>
    <tr>
      <th scope="col">Replies count▼</th>
      <th scope="col">Title</th>
      <th scope="col">Last replied at▼</th>
      <th scope="col">Viewed count▼</th>
    </tr>
  </thead>
  <tbody>
    <%= render partial: "shared/post_list_tbody", locals: {posts: @posts} %>
  </tbody>
</table>
<div class="col-12"><%= paginate @posts, theme: 'twitter-bootstrap-4' %></div>
</div>