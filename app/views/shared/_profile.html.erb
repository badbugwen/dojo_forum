<div class="row">
  <div class="col-md-2 col-sm-4 col-xs-12">
    <div class="text-center">
      <% if user.avatar? %>
        <%= image_tag user.avatar, width: '100px', alt: user.name,  class: "rounded mt-1"  %>
      <% else %>
          <img src="https://placeholdit.imgix.net/~text?txtsize=21&txt=No%20User%20Image&w=100&h=100" class="rounded mt-1">
      <% end %>
      <h2><%= user.name %></h2>
      <%= render partial: "shared/friendship", locals:{user: @user} %> 
    </div>  
  </div>
  <div class="col-md-10 col-sm-8 col-xs-12">
      <%= user.intro %>
      <br>
      <% if current_user == user %>
        <%= link_to 'Edit Profile', edit_user_path(current_user), class: "btn btn-outline-primary" %>
      <% end %>
  </div>
</div>